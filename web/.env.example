# URL del worker desplegado. En local, el uvicorn del contenedor.
WORKER_URL=http://127.0.0.1:8080

# El mismo token que lleva el worker en SERVICE_TOKEN.
# En Vercel va como variable de entorno del proyecto, NO en el repo.
SERVICE_TOKEN=
